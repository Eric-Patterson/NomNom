export default function validateInfo(values) {
    let errors = {}

    if(!values.username.trim) {
        
    } //If username is not in place, there will be this error. Trim to get rid of white space around
}